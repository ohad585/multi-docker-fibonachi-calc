apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-deplyoment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: worker
  template:
    metadata:
      labels:
        component: worker
    spec:
      containers:
      - name: worker
        image: ohad585/multi-worker
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
